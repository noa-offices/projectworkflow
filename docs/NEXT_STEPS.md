# Next Steps

## Immediate

- Fix the visible encoding issues in app labels and metadata text
- Add a single index page inside `docs/` if this folder will be reused often
- Confirm the active quotation status model against the latest business rules

## Next

- Document the quotation approval lifecycle more explicitly
- Add a small route map or sitemap for the most-used ERP screens
- Verify which product-library support tables are part of the active workflow

## Future

- Auto-generate schema and workflow docs from migrations and route structure
- Add end-to-end tests for quotation-to-project and project-to-procurement handoff
- Reduce opaque JSON settings where business validation matters
